$(function() {
    $( "#chat-box" ).resizable({
      maxHeight: 550,
      maxWidth: 650,
      minHeight: 250,
      minWidth: 400,
      handles: 'n'
    });
    $("#chat-box").height("300px");
});
