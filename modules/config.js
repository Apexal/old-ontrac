module.exports = {
  ip: "", // 127.0.0.1 (only you can access the page), your local ip (only your local network can access it), [blank] (if port-forwarded, anyone can access it)
  port: "3000", // Port to run server on
  db: "regis" // MongoDB database to connect to
}
